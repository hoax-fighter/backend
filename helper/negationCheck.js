

const negations = ['tidak', 'tak', 'bukan', 'hampir', 'nyaris', 'gagal'];

const negationCheck = (string, titleArray, similarity) => {
  if (similarity > 80) {
    
  }
}

module.exports = negationCheck;
