const tbhCheck = require('./hoaxChecker');
const webSearch = require('./source');

const posVals = (count) {
  const diminsher = 2;
  const result = {
    vals: [],
    total: 0
  };
}

module.exports = () => {

}
